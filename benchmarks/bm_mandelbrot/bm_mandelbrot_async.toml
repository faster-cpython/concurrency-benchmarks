[project]
name = "mandelbrot_async"
requires-python = ">=3.8"
dependencies = [
    "pyperf",
]
urls = {repository = "https://github.com/faster-cpython/..."}
#urls = {repository = "https://github.com/python/pyperformance"}
dynamic = ["version"]

[tool.pyperformance]
name = "mandelbrot_async"
tags = "apps"
extra_opts = ["async"]
